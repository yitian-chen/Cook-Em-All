# CODEBUDDY.md — 项目方向与约定

> 本文档是给 AI 助手和开发者的项目方向指引。修改本文档前请与项目_owner确认。

## 项目愿景

把现有的吸血鬼幸存者（Vampire Survivors）克隆项目改造为**背包英雄类 + 美食主题**的融合游戏。

- **主角**：厨师
- **武器**：厨房器具（锅铲、勺子、电饭煲、菜刀、擀面杖等）
- **辅助物品**：调味料（盐、胡椒、酱油、辣椒等），放入背包格子给特定格子中的武器提供加成或给玩家提供血量恢复等效果
- **敌人**：各种蔬菜（番茄、洋葱、西兰花、土豆等）

## 核心玩法循环

### 战斗节奏（波次制，类 VS）

- 整局游戏由多个波次组成，波次内是**实时自动战斗**，玩家只负责走位躲避。
- 武器自动开火（沿用 VS 的 Ability 系统），战斗中**无法修改背包**。
- **波次之间**进入整备阶段：玩家可以整理背包、浏览商店、购买物品。
- 单局结束后进入 meta 层：用累计的资源解锁永久内容。

### 背包网格系统（核心新机制）

- **底板（最大范围）**：5 列 × 7 行 = 35 格（暂定，后续策划给出为准）。底板是视觉框，所有位置初始为空（暗色）。
- **两层系统**：
  - **格子层**：玩家在商店购买格子（1×1、1×2、1×3、2×2、2×3 等），拼排在底板上。初始有两个 2×3 格子并排放底板中间。
  - **道具层**：武器和调味料放在格子上方，不能直接放底板。道具尺寸有 1×1、1×2、1×3、2×2、2×3 等，玩家可在背包中自行排列（类似背包英雄/Tarkov 库存）。
- **旋转**：拖拽中按 R 键或鼠标右键可切换物品/格子宽高（如 1×2 ↔ 2×1）。
- **物品尺寸上下文**：实体在商店槽位内以等比缩放的 fit 尺寸显示（维持 gridWidth×gridHeight 宽高比），拖出后立即还原真实背包尺寸。暂存区和背包内均使用真实尺寸。
- **邻接加成**：调味料对相邻格子中的武器产生加成。各个调味料应有**不同的加成机制**（加成范围、效果类型、数值），由策划后续给出。架构上必须做成**数据驱动、可灵活配置**，不能写死任何具体加成规则。

### 商店系统（仅局内）

- **只做局内波次间商店**，不做局外成长、不做局外 meta 商店。
- 波次整备阶段弹出商店，玩家用本局赚的币购买武器/调味料/背包扩格，放入背包。
- 单局结束后不保留任何局外成长，下一局从相同初始状态开始（roguelike 局内循环）。

### 平台目标

- **仅 PC 端**，暂不考虑手机端。UI 可按桌面分辨率设计，无需做触屏适配。

## 与现有架构的关系

### 可直接复用（改动小）

| 现有系统 | 美食主题对应 | 说明 |
|---|---|---|
| `Ability` 基类 + 4 种中间基类（Projectile/Melee/Throwable/Boomerang） | 锅铲/勺子/电饭煲等武器 | 代码基本不动，换 Prefab 和数值即可。详见 `Assets/Scripts/Character/Abilities/` |
| `UpgradeableValue` 升级系统 | 调味料给武器的加成 | 机制匹配，但需扩展为支持"邻接触发"的加成模型 |
| `EntityManager` 对象池 | 蔬菜怪物、食材投射物 | 不动 |
| `MonsterBlueprint` + `LevelBlueprint` 刷怪 | 蔬菜怪物 + 关卡 | 换美术和数值 |
| `CharacterBlueprint` | 厨师主角 | 换美术 |
| 关卡计时、经验、宝箱 | 沿用 | 不动 |

### 需要新建（核心工作）

| 新系统 | 现状 | 优先级 |
|---|---|---|
| **背包网格系统** | ✅ 原型已实现（`Assets/Scripts/Backpack/`：DraggableEntity 基类 + GridTile/DraggableItem 子类 + BackpackGrid 协调器 + 两层占用表），旧 `Inventory.cs` 待淘汰 | P0，核心 |
| **物品放置 + 邻接查询** | ✅ 原型已实现（BackpackGrid 维护 gridTileMap/itemMap 两张占用表，邻接查询基础已具备） | P0，核心 |
| **调味料物品类型 + 加成规则引擎** | 现有升级是"选3选1"升级树，不是可放置实体 | P0，核心 |
| **商店 UI + 交易逻辑** | 拖拽/槽位原型已实现，购买逻辑未做 | P1，仅局内 |
| **波次整备阶段流程** | 现有是连续战斗，无波次间整备 | P1 |
| **主菜单/角色选择扩展** | 现有 MainMenu 较简单 | P2 |

> ❌ **明确不做**：局外 meta 商店、局外永久成长、永久解锁系统。单局结束后无任何跨局养成。

## 技术设计原则

### 1. 调味料加成必须数据驱动

**绝对不能**在代码里写死"盐给相邻武器+10%伤害"这种规则。必须设计成：

- 每种调味料是一个 ScriptableObject（参考现有 `MonsterBlueprint` 模式）
- Blueprint 字段描述：加成范围（四向/八向/同行同列/全局）、加成类型（伤害/攻速/范围/特效）、加成数值、特殊效果（如恢复血量）
- 运行时由通用的"加成计算引擎"读取 Blueprint，遍历背包网格应用加成

这样策划调整加成规则只需改 `.asset` 文件，无需改代码。

### 2. 背包网格与武器系统的桥接

现有武器是 Prefab 挂在 AbilityManager 下自动运行。背包版本需要：

- 背包网格存储"武器物品"数据（不是直接挂 Ability 组件）
- 波次开始时，根据背包内容**实例化对应的 Ability** 到 AbilityManager
- 武器的数值 = 基础数值 + 背包中调味料对该格的加成总和
- 波次结束时销毁 Ability 实例，保留背包数据

### 3. 保留现有的 ScriptableObject 数据驱动模式

新增的武器/调味料/蔬菜怪物都沿用现有的 Blueprint 模式，策划能不改代码就加内容。

## 待策划给出的内容

以下内容由策划后续提供，开发时先用占位数据：

- [ ] 调味料种类清单及各自的加成机制（范围、效果、数值）
- [ ] 武器清单及各自的基础数值、攻击模式
- [ ] 背包格子的扩展价格曲线、初始基础格子数
- [ ] 商店物品的定价、刷新规则
- [ ] 蔬菜怪物的种类、属性、攻击模式
- [ ] 波次数量、每波时长、波次间整备时长

## 开发约定

- **命名空间**：沿用现有的 `Vampire` 命名空间（虽然主题变了，但全局改名风险大，暂保留）。新增的背包相关类可考虑放入 `Vampire.Inventory` 或 `Vampire.Backpack` 子命名空间。
- **代码风格**：沿用现有风格（`[SerializeField] private` 字段、`PascalCase` 属性方法、`camelCase` 局部变量）。
- **数据驱动优先**：任何可能由策划调整的数值/规则，都必须做成 ScriptableObject，不能硬编码。
- **PC 端优先**：UI 按桌面分辨率设计，使用鼠标拖拽放置物品。

## 参考文档

- 武器/能力系统架构：详见 `Assets/Scripts/Character/Abilities/Ability.cs` 及其子类
- 升级值系统：详见 `Assets/Scripts/Utilities/UpgradeableValues.cs`
- 关卡配置：详见 `Assets/Scripts/ScriptableObjects/LevelBlueprint.cs`
- 背包两层系统：详见 `Assets/Scripts/Backpack/`（DraggableEntity / GridTile / DraggableItem / BackpackGrid / BackpackSlot / ShopSlot / StagingArea）
- 原型场景：`Assets/Scenes/Game/Shop Prototype.unity` + `Assets/Scripts/Prototype/ShopPrototype.cs`
- 旧 Inventory（待淘汰）：`Assets/Scripts/Gameplay/Inventory/Inventory.cs`
